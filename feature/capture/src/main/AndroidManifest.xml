<?xml version="1.0" encoding="utf-8"?>
<!--
    Le fournisseur de fichiers par lequel l'appareil photo du systeme ecrit la photo.

    L'autorite derive de l'applicationId, donc elle differe entre debug et release :
    deux variantes installees cote a cote ne peuvent pas se disputer la meme.

    La permission CAMERA n'est pas declaree ici : c'est :integration:scanner qui la
    porte, et la fusion des manifestes l'apporte a l'APK. La declarer deux fois ne
    changerait rien a ce que l'utilisateur voit, mais ferait deux endroits a corriger.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.capture"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/capture_paths" />
        </provider>
    </application>

</manifest>
